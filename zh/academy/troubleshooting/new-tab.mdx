---
title: "新标签页处理"
description: "识别网页是否使用新标签页打开链接，并在八爪鱼采集器中正确配置新标签页的采集方法。"
---

# 新标签页处理

当我们进行数据采集，常常需要从当前页面点击到下级页面。当我们执行点击元素时，需要判断网页是否用了新标签，并进行相关设置。

## 一、什么是新标签？

新标签页通常指当我们执行**点击**操作时，在浏览器中打开一个新的网页来显示特定的内容，并在新标签页中加载新的网页地址。不同于 Ajax 只需要更新部分网页，此类网站如果需要更新内容，必须重新加载整个网页页面。

## 二、如何判断网页是否使用了新标签

1. 在浏览器里打开网页，如果点击后在浏览器中打开了一个全新的标签页，且地址栏显示的是新的 URL，即可判断网页使用了新标签。
2. 在客户端里点击后整个页面重新加载（点击后浏览器网址栏开始转圈，等待一段时间后转圈停止），转圈停止后有新的数据出现，即该网页也使用了新标签。

## 三、如何设置新标签

判断该网页使用了新标签后，我们可以在【点击元素】步骤的设置中找到新标签设置，选中该框以启用它，反之亦然。不要忘记单击应用保存你的更改。

<Warning>
  如果该网页没有使用新标签，但我们进行了勾选，当我们执行点击元素步骤后的提取数据步骤，页面会白屏，网址显示 `about:blank`，此时我们将新标签取消勾选即可。
</Warning>
